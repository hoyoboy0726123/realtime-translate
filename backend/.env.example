# Copy to .env and fill in.

# Required for the "cloud" engine (OpenAI Realtime API).
OPENAI_API_KEY=
OPENAI_REALTIME_MODEL=gpt-realtime-translate

# Comma-separated list of allowed frontend origins.
CORS_ORIGINS=http://localhost:3000
